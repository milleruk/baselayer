"""
Models for the classes app.

The main model (RideDetail) remains in workouts.models for backward compatibility
and because it's used by both workout history and class library functionality.
"""
from django.db import models

